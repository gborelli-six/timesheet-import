# Variabili frontend (prefisso VITE_ — esposte al bundle client-side)
# ATTENZIONE: non inserire secret in variabili VITE_*

# URL base API (non usato in prod: si usano path relativi /api/...)
# VITE_API_BASE_URL=

# Google OAuth Client ID (pubblico — non è un secret)
VITE_GOOGLE_CLIENT_ID=
